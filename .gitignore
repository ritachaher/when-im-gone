node_modules
dist
dist-ssr
dist-mobile-check
.env
.env.local
.env.*.local
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log
coverage
*.timestamp-*
tsconfig.tsbuildinfo
# Stray JS emitted alongside TS sources (tsc accidental emit). Vite builds
# from the .ts sources directly, so these are never needed.
src/**/*.js
